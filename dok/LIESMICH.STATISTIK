

INSTALLATION
************

Nach dem auspacken des Archivs muss mit mysqladmin eine neue Datenbank angelegt
und initialisiert werden. Die Definitionen der Datenbank befinden sich im Ver-
zeichnis 'doc' unter 'statistic.mysql.def':

  $ mysqladmin create statistiken
	$ mysql statistiken <statistic.mysql.def
	
In diesem Beispiel ist der Name der Datenbank 'statistiken'. Dieser Name muss
in der Konfiguration (zu finden unter 'statistiken/etc/config.php') ange-
paßt werden.
